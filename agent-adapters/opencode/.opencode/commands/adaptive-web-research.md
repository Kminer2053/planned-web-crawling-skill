---
description: Plan and execute reproducible web research with the adaptive-web-research toolkit
agent: plan
subtask: false
---

Use the toolkit in @.agent-skills/adaptive-web-research to handle this task:

$ARGUMENTS

Read these first:

- @.agent-skills/adaptive-web-research/SKILL.md
- @.agent-skills/adaptive-web-research/references/collection-workflow.md

Workflow:

1. Identify the most official start URL or file.
2. Probe the source before broad collection.
3. If the source requires repeated requests, create a JSON plan and run the plan runner.
4. Save raw snapshots in `tmp/adaptive-web-research/<task-name>/`.
5. Report the exact commands, URLs, request methods, and saved paths.
